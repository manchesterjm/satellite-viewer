// Template — copy to config.local.js and fill in.
// Get a free CIMSS RealEarth access key at https://realearth.ssec.wisc.edu/users
// to remove "Size limit exceeded" watermarks from cloud-cover tiles. Without
// a key, anonymous use gets watermarked tiles. Whitelist your viewer's URL
// (e.g. http://localhost:8000) in your account's allowed-referers list.
window.CIMSS_ACCESS_KEY = "";
