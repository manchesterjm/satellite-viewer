# TLE data file is regenerated every 3 days by Refresh-TLEs.ps1 — don't track
data/*.tle
data/*.txt

# Local secrets (CIMSS API key, etc.) — see config.local.example.js for template
config.local.js

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~
