# TLE data file is regenerated every 3 days by Refresh-TLEs.ps1 — don't track
data/*.tle
data/*.txt

# Cloud-forecast cache written at runtime for pass viewability — don't track
data/*.json

# Local secrets (CIMSS API key, etc.) — see config.local.example.js for template
config.local.js

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~
